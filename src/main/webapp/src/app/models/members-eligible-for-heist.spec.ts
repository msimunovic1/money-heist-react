import { MembersEligibleForHeist } from './members-eligible-for-heist';

describe('MembersEligibleForHeist', () => {
  it('should create an instance', () => {
    expect(new MembersEligibleForHeist()).toBeTruthy();
  });
});
